enum TrimMode {
  /// When mode is `none, then we will show max possible characters in
  /// available rendering space.
  none,
  character,
  line,
}

enum ReadMoreState {
  collapsed,
  expanded,
}
